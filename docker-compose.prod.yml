version: '2'
services:
  web:
    # build: .
    image: petclinic/petclinic:latest
    ports:
      - '8080:8080'
    environment:
      - DB_HOST=mysql:3306
      - DB_USERNAME=root
      - DB_PASSWORD=
      - INIT_DB=true
    restart: on-failure
